# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: '3.44'

tasks:
  # move this to the root Taskfile
  asdf-install:
    internal: true
    run: once
    cmds:
      - ./.taskfiles/asdf-plugins.sh
      - asdf install
    sources:
      - ".tool-versions"


