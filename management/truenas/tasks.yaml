# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: "3.44"

tasks:
  gen-portainer-compose:
    desc: Generate the Portainer Docker Compose file
    deps:
      - :asdf-install
    cmds:
      - op inject -i docker-compose.tmpl.yaml -o docker-compose.yaml
