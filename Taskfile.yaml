# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: "3.44"

includes:
  asdf:
    taskfile: ./.taskfiles/tasks-asdf.yaml
    flatten: true

  cloudflare:
    taskfile: ./management/cloudflare/tasks.yaml
    dir: management/cloudflare

  portainer:
    taskfile: ./management/portainer/tasks.yaml
    dir: management/portainer

  truenas:
    taskfile: ./management/truenas/tasks.yaml
    dir: management/truenas
